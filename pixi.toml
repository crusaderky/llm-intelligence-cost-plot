[workspace]
authors = ["crusaderky <crusaderky@gmail.com>"]
channels = ["conda-forge"]
name = "llm-intelligence-cost-plot"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
plot = "python plot.py"
lint = "ruff check --fix && ruff format && mdformat ."

[dependencies]
mdformat = "*"
matplotlib = "*"
ruff = "*"
