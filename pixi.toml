[workspace]
authors = ["crusaderky <crusaderky@gmail.com>"]
channels = ["conda-forge"]
name = "llm-intelligence-cost-plot"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
plot = "python plot.py"
aa-query = "python .pi/skills/aa-lookup/scripts/aa_query.py"
lint = "ruff check --fix && ruff format && mdformat ."

[dependencies]
mdformat = "*"
matplotlib = "*"
ruff = "*"
